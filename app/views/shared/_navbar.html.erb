<div class="navbar squared-navbar">
  <div class="navbar-inner">
    <div class="container-fluid">
      <%= link_to "Saturn", root_path, class: "brand" %>
      <div class="brand title"><%= yield :title %></div>
      <ul class="nav pull-right">
        <%= render "shared/top_nav_menu" %>
        <% if can? :manage, :all %>
          <li class="divider-vertical"></li>
          <li class="dropdown">
            <%= render "shared/admin_menu" %>
          </li>
        <% end %>
        <li class="divider-vertical"></li>
        <%= render "shared/personal_menu" %>
      </ul>
    </div>
  </div>
</div>
